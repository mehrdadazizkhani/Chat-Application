import React from "react";

interface ChatAreaProps extends React.PropsWithChildren {}

export const ChatArea: React.FunctionComponent<
  ChatAreaProps
> = (): JSX.Element => {
  return (
    <section className="w-screen h-[calc(100vh-100px)] bg-slate-500">
      salamgffg
    </section>
  );
};
